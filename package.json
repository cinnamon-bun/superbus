{
  "name": "superbus",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "repository": "",
  "author": "Cinnamon <cinnamon_bun_github@fastmail.com>",
  "homepage": "https://github.com/cinnamon-bun/suberbus",
  "license": "AGPL-3.0",
  "scripts": {
    "clean": "rm -rf build coverage .nyc_output",
    "build": "tsc",
    "start": "node build/index.js",
    "test": "tap -Rspecy --jobs=1 build/test/*.test.js",
    "build-and-test": "yarn build && yarn test",
    "watch-test": "onchange -i src/*.ts src/test/*.ts -- yarn build-and-test",
    "test-coverage": "yarn test -- --coverage-report=lcov",
    "prepublishOnly": "yarn run clean && yarn run build"
  },
  "devDependencies": {
    "@types/tap": "^14.10.2",
    "onchange": "^7.1.0",
    "tap": "^14.11.0",
    "typescript": "^4.1.4"
  },
  "dependencies": {}
}
